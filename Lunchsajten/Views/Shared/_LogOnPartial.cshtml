@*@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Logga ut", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Logga in", "LogOn", "Account") ]
}
*@

@if(Request.IsAuthenticated) {
    @Html.ActionLink("Logga ut", "LogOff", "Account")
}
else {
    @Html.ActionLink("Logga in", "LogOn", "Account")
}